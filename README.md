# envelope
Encrypted dotenvx .env files as first-class Nix deployment artifacts, for NixOS and nix-darwin behind one shared option tree (services.dotenvx). Includes the postmaster Rust credential daemon for credentials mode.
